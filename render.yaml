services:
  - type: web
    name: mbn-telemetry-gateway
    runtime: node
    plan: free
    region: singapore
    buildCommand: npm install
    startCommand: node server/gateway.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
